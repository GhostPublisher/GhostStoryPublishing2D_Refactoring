/*using UnityEngine;

namespace GameSystems.Modules.Inputs
{
    public class Battle_GameSystemInputController : MonoBehaviour, IInputController
    {
        // 각 Flow로 연결하기 위해서, 이곳에선 각 시스템 Controller를 참조한다.

        private void Awake()
        {
            
        }

        private void Update()
        {
            if (Input.GetKeyDown(KeyCode.Escape)) { }

            if (Input.GetKeyDown(KeyCode.Alpha1)) { }

            if (Input.GetKeyDown(KeyCode.Alpha2)) { }

            if (Input.GetKeyDown(KeyCode.Alpha3)) { }

            if (Input.GetKeyDown(KeyCode.Space)) { }
        }

        // Unity Event Flow 접근을 활성화 시킴.
        public void ActivateUnityEventFlow()
        {
            this.enabled = true;
        }
        // Unity Event Flow 접근을 비활성화 시킴.
        public void DisActivateUnityEventFlow()
        {
            this.enabled = false;
        }
    }
}*/